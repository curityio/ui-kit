{
  "name": "@curity/ui-kit-css",
  "version": "1.0.0",
  "description": "Curity CSS library",
  "author": "Curity",
  "license": "Apache-2.0",
  "private": true,
  "main": "index.js",
  "scripts": {
    "prestart": "test \"$SKIP_PRESTART\" = \"1\" || npm run build:lib",
    "start:docs": "npm start -w docs",
    "start:lib": "npm start -w lib",
    "start": "npm-run-all -p start:docs start:lib",
    "build:docs": "npm run build -w docs",
    "build:lib": "npm run build -w lib",
    "build:all": "npm-run-all -s build:lib build:docs",
    "build": "npm run build:lib"
  },
  "dependencies": {
    "npm-run-all": "^4.1.5"
  }
}
