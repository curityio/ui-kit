diff --git a/src/devops-dashboard-backstage-app/app-config.local.yaml b/src/devops-dashboard-backstage-app/app-config.local.yaml
new file mode 100644
index 0000000..3c1f250
--- /dev/null
+++ b/src/devops-dashboard-backstage-app/app-config.local.yaml
@@ -0,0 +1,15 @@
+# Local dev overrides (gitignored). Created by:  git apply src/devops-dashboard-backstage-app/dev-secrets.patch
+auth:
+  session:
+    secret: backstage-dev-session-secret
+  providers:
+    oidc:
+      development:
+        clientSecret: Password1 # must match the client registered on the dev server (see PR test instructions)
+
+# If port 3000 is taken (e.g. the CRA DevOps Dashboard dev server), uncomment:
+# app:
+#   baseUrl: http://localhost:3001
+# backend:
+#   cors:
+#     origin: http://localhost:3001
