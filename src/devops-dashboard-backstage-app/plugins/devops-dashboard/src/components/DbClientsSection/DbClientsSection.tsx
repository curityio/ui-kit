/**
 * Static placeholder for the Database Clients section. The real content
 * (table, search, states) lands in the next step; this only proves the
 * plugin structure: sidebar entry → page → tab → section.
 */
export const DbClientsSection = () => (
  <section style={{ padding: '1rem' }}>
    <h2>Database Clients</h2>
    <p>Static section placeholder — content arrives in the next commit.</p>
  </section>
);
