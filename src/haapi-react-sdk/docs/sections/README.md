# HAAPI React (Web) SDK

Render the Curity Identity Server's server-driven (HAAPI) authentication flows in React. The server describes each step; the SDK renders it and advances the flow. Every page below embeds a live, editable playground.
